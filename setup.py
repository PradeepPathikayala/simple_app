from setuptools import setup, find_packages


setup(
    name='src',
    version = '0.0.1',
    description='its a simple app built uisng dvc mlops',
    author = 'Pradeep Pathikayala',
    packages = find_packages(),
    license = 'MIT'

)

